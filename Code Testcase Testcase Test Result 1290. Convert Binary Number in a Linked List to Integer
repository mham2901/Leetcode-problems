class Solution {
public:
    int getDecimalValue(ListNode* head) {
        
        int number = 0;

        while (head != nullptr) {
            number = number * 2 + head->val;  
            head = head->next;
        }
        return number;
    }
};
